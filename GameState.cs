using UnityEngine;
using BGC.StateMachine;

public class GameState : TriggeringUpdatingLambdaState<StateTrigger>
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
